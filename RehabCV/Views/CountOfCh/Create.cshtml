@model RehabCV.DTO.CountOfChildrenDTO
@{ ViewData["Title"] = "Кількість дітей в групі"; }

@await Html.PartialAsync("_IndexHeader")

<div class="row">
    <div class="col-md-4" style="margin: auto">
        <h2 style="text-align: center">@ViewData["Title"]</h2>
        <form method="post" asp-controller="CountOfCh" asp-action="Create">
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CountOfChildren"></label>
                <input asp-for="CountOfChildren" class="form-control" />
                <span asp-validation-for="CountOfChildren" class="text-danger"></span>
            </div>
            
            <button type="submit" class="btn btn-primary" style="width: 100%">Створити</button>
        </form>
    </div>
</div>

@await Html.PartialAsync("_IndexFooter")

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}