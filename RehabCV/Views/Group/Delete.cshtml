@model RehabCV.DTO.GroupDTO

@{
    ViewData["Title"] = "Видалити";
}

@await Html.PartialAsync("_IndexHeader")

<h2  style="margin-top: 12%; text-align: center; font-family: Geometria; font-weight: 700; font-size: 40px; color: #061e37;">@ViewData["Title"]</h2>

<p>
     <a asp-action="Index">Назад до списку</a>
</p>

<div>
    <div class="modal-content">
        <div class="modal-body" style="margin: 10px">
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.NameOfDisease)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.NameOfDisease)
                </dd>
            </dl>

            <form asp-action="Delete">
                <input type="submit" value="Видалити" class="btn btn-danger" style="width: 30%; margin-left: 35%" />
            </form>
        </div>
       
    </div>
</div>

@await Html.PartialAsync("_IndexFooter")
